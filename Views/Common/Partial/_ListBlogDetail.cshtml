@model List<ketnoicungcau.business.Model.News>

@{
    if (Model != null && Model.Any())
    {
        foreach (var item in Model)
        {
            <partial name="~/Views/Home/Partial/Info.cshtml" model="@item" />
        }
    }
}

